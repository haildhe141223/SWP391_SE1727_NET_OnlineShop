@using SWP391.OnlineShop.Core.Models.Enums;
@model List<SWP391.OnlineShop.ServiceModel.ViewModels.Products.PostViewModel>

<div class="m-headers ml-3" style="margin-top: 10% !important;">
    <h4 class="fw-bold">Post Management</h4>
    <a asp-area="Managements" asp-controller="Marketing" asp-action="AddPost">
        <button class="btn btn-primary">
            <span>Add</span>
            <i class="fa-regular fa-plus"></i>
        </button>
    </a>

</div>

<div class="card shadow mb-4 mt-3">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Posts DataTables</h6>
    </div>
    <div class="card-body container-fluid">
        <div class="table-responsive">
            <table class="table table-hover table-bordered" id="dataTable" width="100%" cellspacing="0">
                <thead class="table-secondary">
                    <tr>
                        <th scope="col">Title</th>
                        <th scope="col">Brief</th>
                        <th scope="col">Featured</th>
                        <th scope="col">Thumbnail</th>
                        <th scope="col">Description</th>
                        <th scope="col">Author</th>
                        <th scope="col">Status</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var postViewModel in Model)
                    {
                        <tr>
                            <td>@postViewModel.Title</td>
                            <td>@postViewModel.Brief</td>
                            <td>@postViewModel.Featured</td>
                            <td><img width="300" height="300" src="@postViewModel.Thumbnail"></td>
                            <td>@Html.Raw(@postViewModel.Description)</td>
                            <td>@postViewModel.Author</td>
                            @if (postViewModel.Status == Status.Active)
                            {
                                <td class="text-success">Active</td>
                            }
                            else
                            {
                                <td class="text-danger">InActive</td>
                            }
                            <td>
                                <div class="table-action">
                                    <a asp-area="Managements" asp-controller="Marketing" asp-action="ViewPost" asp-route-id="@postViewModel.Id">
                                        <i class="fa-regular fa-eye"></i>
                                    </a>
                                    <a asp-area="Managements" asp-controller="Marketing" asp-action="EditPost" asp-route-id="@postViewModel.Id">
                                        <i class="fa-regular fa-pen-to-square text-primary"></i>
                                    </a>
                                    <a asp-area="Managements" asp-controller="Marketing" asp-action="DeletePost" asp-route-id="@postViewModel.Id">
                                        <i class="fa-regular fa-trash-can text-danger"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>